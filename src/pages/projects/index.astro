---
import ProjectsPageView from '../../components/pages/ProjectsPage.astro';
import { getPage, type ProjectsPage } from '../../lib/sanityPages';

const page = await getPage<ProjectsPage>('projectsPage');
---

<ProjectsPageView page={page} />
